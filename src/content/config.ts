// Content collections disabled. The project uses static `src/data/` files
// instead of Astro Content collections. Export an empty collections
// object so the content plugin does not expect `src/content/*` folders.
export const collections = {};
